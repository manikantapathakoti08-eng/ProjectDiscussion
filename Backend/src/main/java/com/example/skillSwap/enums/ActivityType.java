package com.example.skillSwap.enums;


public enum ActivityType {
    LOGIN,
    LOGOUT,
    SIGNUP,
    TOPIC_ADDED,      // When a user adds a topic to their profile
    SESSION_ACCEPTED, // When a guide accepts a marketplace request
    REFUND_ISSUED,    // When an admin refunds a session
}